[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
msw = { path = "../MSW" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[profile.release.package.aes]
opt-level = 0
